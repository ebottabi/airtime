$(document).ready(function() {
    // to cache SDK
    /*$.ajaxSetup({ cache: true });

    // imports the SDK from the correct URL for your user’s locale
    $.getScript('//connect.facebook.net/en_US/sdk.js', function() {
        // FB object is global so it can be used outside of this function.
        // Just make sure it exists before using it.
        FB.init({
            // do SDK initialization here
        });
    });*/
});